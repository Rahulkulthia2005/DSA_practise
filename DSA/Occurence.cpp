#include<iostream>
using namespace std;
int main()
{
    String str="daabcbaabcbc";
    String part="abc";
    while(str.length()>0&& )
    str.find(abc)<s.length();
    {
        count++;
    }
    str.erase(str.find(part),part.length())
}